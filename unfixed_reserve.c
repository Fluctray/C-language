#include<stdio.h>
#define N 10
int main(){
    int a[i], *p;
    printf("Enter arrys:");
    for(p = &a[0]; p<&a[i]; p++){
        scanf("%d",&a[i]);
    }
    for(p = &a[0]; p<&a[i]; p++){
    //for(p = &a[N]; p>&a[0]; p--){
        printf("%d", a[i]);
    }
}
